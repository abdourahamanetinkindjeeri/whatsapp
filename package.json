{
  "name": "whatsapp-project",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "cd src/server && node server.js",
    "build": "cd src/server && npm install"
  }
}
